 
import { StyleSheet } from 'react-native';

const GlobalStyles = StyleSheet.create({
  text: {
    fontFamily: 'OpenSansRegular', // Cambia esto si usas un nombre diferente
  },
});

export default GlobalStyles;
